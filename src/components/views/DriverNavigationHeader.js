import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class DriverNavigationHeader extends Component {
  render() {
    return (
      <View>
        <Text>Driver Navigation Header!</Text>
      </View>
    );
  }
}